const hostname = 'localhost'
// const hostname = '192.168.68.189'
// const hostname = '129.28.148.128'
const port = 5000
const baseUrl = `http://${hostname}:${port}/`

const appConfig = {
  hostname,
  port,
  baseUrl
}

export default appConfig
